# Core
torch>=2.2
torchvision>=0.17
numpy>=1.26
pandas>=2.1
pyarrow>=15.0
pillow>=10.0
tqdm>=4.66

# Model zoo
timm>=1.0.7
transformers>=4.44

# Analysis
scikit-learn>=1.4
matplotlib>=3.8
# rsatoolbox is only needed for the Phase 5 RSA figure and requires
# scikit-learn>=1.6 (it imports sklearn.utils.validation.validate_data).
# Install it in a fresh env when you reach Phase 5, or compute RDM
# correlations directly with scipy. Pinned out here to avoid forcing a
# global sklearn upgrade.
# rsatoolbox>=0.2.0  ; needs scikit-learn>=1.6

# Data download (OSF)
requests>=2.31
